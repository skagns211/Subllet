// import {  } from "../actions/index";

const userInfo = {};

const userReducer = (state = userInfo, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default userReducer;
