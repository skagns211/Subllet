import React from "react";
import styled from "styled-components";

const MyScrapContainer = styled.main`
  width: 100%;
  height: 30rem;
  margin-top: 2rem;
  border: 0.5px solid white;
  /* flex-basis: auto; */
`;

const MyScrap = () => {
  return <MyScrapContainer>hihi</MyScrapContainer>;
};

export default MyScrap;
